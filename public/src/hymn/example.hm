
function foo(x, y)
  list = []
  table = {}
  if x != 0
    print(x)
  else
    y += 1
  end
  list.push(4)
  table['key'] = list
  return y
end

function bar()
  for i = 0, i < 10, i += 1
    if i < 0
        continue
    end
  end
end

function zoo()
  x = 0
  closure = function()
    x += 1
  end
  closure()
end

foo(2, 3)
