
interface goo
  get()
  set()
end

class zoo implements goo
  constructor()
    this.x = 0
    this.y = "${this.x}"
  end
end

function foo(x, y)
  list = []
  table = {}
  if x != 0
    print(x)
  else
    y += 1
  end
  list.push(4)
  table['key'] = list
  return y
end

function bar()
  for i = 0, i < 10, i += 1
    if i < 0 continue end
  end
end

foo(2, 3)
